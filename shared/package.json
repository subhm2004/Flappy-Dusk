{
  "name": "@flappy/core",
  "version": "1.0.0",
  "private": true,
  "description": "Pure, deterministic game core. Shared by the game and by the backend, which replays runs with it to verify submitted scores.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
